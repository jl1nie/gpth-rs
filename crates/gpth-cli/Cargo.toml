[package]
name = "gpth-cli"
version = "0.4.0"
edition = "2021"

[[bin]]
name = "gpth-rs-cli"
path = "src/main.rs"

[dependencies]
gpth-core = { path = "../gpth-core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
ctrlc = { version = "3.4", features = ["termination"] }
